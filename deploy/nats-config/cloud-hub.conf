# "Cloud" nearest-server hub for the client-cloud deployment model (no
# on-prem tier — the daemon connects directly to this cluster).
port: 4222
server_name: nats-hub-cloud

jetstream {
    store_dir: "/data"
}

leafnodes {
    port: 7422
}
