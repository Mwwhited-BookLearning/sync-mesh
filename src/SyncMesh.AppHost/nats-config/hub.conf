# Nearest-server NATS cluster ("hub" side of the leaf-node connection).
# See docs/adr/0002-nats-leaf-nodes-for-transport.md.
port: 4222
server_name: nats-hub

jetstream {
    store_dir: "/data"
}

leafnodes {
    port: 7422
}
