# client-onprem deployment model's daemon leaf node — dials out to this
# model's own hub. See docs/adr/0002-nats-leaf-nodes-for-transport.md.
port: 4222
server_name: nats-leaf-onprem

jetstream {
    store_dir: "/data"
}

leafnodes {
    remotes: [
        { url: "nats-leaf://nats-hub-onprem:7422" }
    ]
}
