# Site A daemon's embedded local NATS leaf node — dials out to site A's
# nearest-server hub. See docs/adr/0002-nats-leaf-nodes-for-transport.md.
port: 4222
server_name: nats-leaf-a

jetstream {
    store_dir: "/data"
}

leafnodes {
    remotes: [
        { url: "nats-leaf://nats-hub-a:7422" }
    ]
}
